<% content_for :title,
  safe_join([admin_nav_books_link, admin_nav_book_link(@book), "Edit"], " > ")
  %>

<div class="container mt-4">
  <%= render "form", book: @book, form_handler: @form %>
</div>
